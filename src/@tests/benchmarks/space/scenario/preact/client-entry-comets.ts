/// <reference lib="dom" />
import { hydrateComets } from 'modules/client/mod-preact.ts'

// Preact counterpart to `react/client-entry-comets.ts` — same reasoning, same real, unmodified
// Space bootstrap call.
hydrateComets()
